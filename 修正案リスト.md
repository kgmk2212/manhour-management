- ✅ レポートのマトリクス表示を見やすくする。見積と実績が一目でわかりにくい。また赤緑はセル全体に色をつけるようにして、乖離率によってグラデーションにする
- 担当者別集計の充実。見積一覧にも表示。レポートも集計を増やす。全てに人日、人月表示
- レポートのグラフを増やして各グラフは小さく表示
- ガントチャート機能追加
- ✅ レポートの月別/版数別のフィルタ選択のボタンだけ設定色の変更反映がされない。選択を変えると初めて反映される
- ✅ 見積一覧のカードの左ライン色が設定反映されていない
- 見積新規登録で複数月の時の作業月デフォルト選択について、終了月は後工程の前の月になるようにする？工数から換算して自動で出す？？
- ✅ 見積一覧は全期間をデフォルト表示に。→月別/版数別を実装し、版数別の最新をデフォルトにする
- ✅ 見積一覧の月別も全期間と同じようにセルに色つける
- ✅ 見積登録の時、PGを入力したら同じ担当者をPTに、ITを入力したらSTに自動入力するようにする
- ✅ 見積登録の帳票名の候補がモバイルで表示されない
- クイック入力タブは要らなくなる（後回し：実装案あり）
  【実装案】クイック入力タブの実績入力機能を実績一覧に統合する
  - 現状：クイック入力タブの対応検索UIが便利だが、カレンダーからの実績登録（editActualModal）には同様のUIがない
  - 課題：2つの実績登録方法を統合する必要がある
    1. クイック入力：対応検索ドロップダウン + 担当者選択 + 日付今日固定
    2. カレンダー：対応selectのみ + 担当者・日付は決定済み
  - 実装手順：
    1. editActualModalに対応検索ドロップダウンUIを追加（filterQuickTaskList等のロジックを統合）
    2. カレンダーから開く場合は担当者・日付固定、直接開く場合は選択可能に
    3. クイック入力タブの見積登録・休暇登録機能は既に他で代替可能
    4. テスト後、クイック入力タブを削除
  - 影響範囲：editActualModal, quickAddActual関連の関数、UI/UX設計
- 見積登録の複数月分割登録は、手動分割設定ができるようになれば要らなくなるので統一方法を考える
- 見積一覧の編集モードなくす？常に編集可能にする（後回し：現状維持）
- 見積一覧から編集で割り当て月を変更できるようにする
- 見積のない対応も実績の候補に出すようにする。ただし　見積登録がないことを表示する。
- リスト表示を条件指定してフィルターできるようにする。
- 登録、編集系の全てのモーダルでキーボード操作ができるように、タブキーでボタンにフォーカス出来るようにする
- レポートから対応する実績カレンダーの箇所にジャンプまたはモーダルで表示し、編集できるようにする。
- レポートから見積にジャンプまたは編集できるようにする。
- UI工程の工数から自動で判定した対応規模に応じて、標準工数を過去のデータから算出してアシスト的に表示する
- 見積一覧から編集する際に対応ごと変更、削除が可能にする→実装済みだが、絵文字等変更予定。編輯モード押さなくても良いようにするか、常に画面上に表示する？
- 月別フィルタのセグメントボタン横スクロールできるようになっていない
- フィルタ設定部分は下にスクロールした時も常に画面上部に残して表示するようにする
- UI工程の工数から自動で判定した対応規模に応じて、標準工数を過去のデータから算出してアシスト的に表示する
- ✅ 設定がバックアップデータに保存されていない
- その他作業の見積を作成し、集計に含められるようにする
- 見積一覧でレポート版数別から月別に戻った時に何もデータが表示されない。月を選べば表示される。
- 見積一覧とレポートのフィルタ選択と版数/月選択デフォルトどうなっているか、バグがないか再確認
- 見積一覧の編集モードが重い
- データ増える一方なので、分割方法など運用の仕方を考える。
- モーダルが最初閉じるバグ。詳細は確認中
- フィルタ等表示条件を変えたあとレポートタブを開くとグラフが表示されないことがある。表示条件を変えると表示される。